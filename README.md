# File Encryptor

Десктопное и мобильное приложение для хранения и шифрования файлов. Создано на [Tauri](https://tauri.app/) + [Nuxt](https://nuxt.com/) + [Vuetify](https://vuetifyjs.com/).

**Разработчик:** [QUGOR](https://github.com/Matatabe23)

---

## Что делает приложение

- **Коллекции** — создание папок двух типов:
  - **Зашифрованные коллекции** — файлы шифруются паролем (AES-256-GCM, ключ из пароля через PBKDF2). Пароль нигде не хранится; ключ расшифровки получается при вводе пароля и живёт только в сессии.
  - **Хранилище** — обычные коллекции без шифрования.
- **Файлы** — добавление, просмотр, удаление файлов в коллекциях. Для зашифрованных коллекций — разблокировка по паролю и расшифровка на лету.
- **Экспорт/импорт** — скачивание коллекции в виде ZIP (с опцией шифрования) и импорт такой коллекции из ZIP.
- **Локализация** — поддержка русского и английского языков.
- **Тема** — светлая и тёмная тема.

---

## Платформы

| Платформа   | Статус        |
|------------|----------------|
| **Android** | ✅ Поддерживается |
| **Windows** | ✅ Поддерживается |
| **iOS**     | 🔜 В планах     |
| **macOS**   | 🔜 В планах     |

Сборка под iOS и macOS выполняется только на macOS; под Windows — только на Windows (см. скрипты в `package.json`).

---

## Требования

- [Node.js](https://nodejs.org/) (LTS)
- [Rust](https://www.rust-lang.org/) (для Tauri)
- Для **Android**: Android SDK, NDK, Java  
- Для **iOS/macOS**: Xcode (только на macOS)  
- Для **Windows**: Visual Studio Build Tools с компонентом «Разработка классических приложений на C++»

---

## Установка

```bash
npm install
```

---

## Команды (npm scripts)

### Разработка и сборка фронтенда

| Команда | Описание |
|--------|-----------|
| `npm run dev` | Запускает Nuxt dev-сервер на `http://localhost:3000` для разработки интерфейса. |
| `npm run build` | Собирает Nuxt-приложение для production (без Tauri). |
| `npm run generate` | То же, что `build` — генерирует статический вывод в `.output/public`. |
| `npm run preview` | Локальный просмотр production-сборки Nuxt. |
| `npm run postinstall` | Выполняется после `npm install`; запускает `nuxt prepare` (подготовка Nuxt). |

### Tauri (десктоп/мобильное приложение)

| Команда | Описание |
|--------|-----------|
| `npm run tauri` | Вызов CLI Tauri без аргументов (можно дописать, например: `npm run tauri dev`). |
| `npm run tauri:dev` | Запуск приложения в режиме разработки: поднимается Nuxt dev и открывается окно Tauri с hot-reload. |
| `npm run tauri:build` | Сборка нативного приложения для текущей ОС (Windows/macOS/Linux). |
| `npm run tauri:generate-icons` | Генерация иконок приложения из `src-tauri/icons/icon.png`. |

### Сборка под платформы (артефакты в `build/<platform>/`)

| Команда | Описание |
|--------|-----------|
| `npm run android:package` | Генерация иконок → копирование в Android → сборка Tauri для Android → копирование APK/AAB в `build/android/`. |
| `npm run android:store` | То же, что `android:package` (алиас для публикации в магазин). |
| `npm run ios:package` | Сборка для iOS (только на macOS). Генерация иконок → `tauri ios build` → копирование в `build/ios/`. |
| `npm run ios:store` | То же, что `ios:package`. |
| `npm run macos:package` | Сборка для macOS (только на macOS). Иконки → `tauri build` → копирование DMG/app в `build/macos/`. |
| `npm run macos:store` | То же, что `macos:package`. |
| `npm run windows:package` | Сборка для Windows (только на Windows). Иконки → `tauri build` → копирование NSIS/MSI в `build/windows/`. |
| `npm run windows:store` | То же, что `windows:package`. |

### Массовые сборки

| Команда | Описание |
|--------|-----------|
| `npm run build:all` | Последовательно запускает `android:package`, `ios:package`, `macos:package`, `windows:package`. На неподходящей ОС соответствующие шаги пропускаются (например, iOS/macOS только на macOS). |
| `npm run release:all` | То же, но через `*:store` (алиас под релизы/магазины). |

---

## Структура проекта (кратко)

- `app/` — Nuxt-приложение (страницы, компоненты, сторы, локали, хелперы).
- `src-tauri/` — Rust-код Tauri (иконки, конфиг, плагины).
- `scripts/` — вспомогательные скрипты:
  - `run-platform-build.js` — сборка под выбранную ОС (ios/macos/windows) с проверкой платформы.
  - `copy-to-build.js` — копирование артефактов Tauri в `build/<platform>/`.
  - `copy-android-icons.js` — копирование иконок для Android.
- `build/` — готовые артефакты сборки по платформам (создаётся при запуске `*:package` / `*:store`).

---
